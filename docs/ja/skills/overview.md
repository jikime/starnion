---
title: スキルシステム
nav_order: 1
parent: スキルシステム
grand_parent: 🇯🇵 日本語
---

# スキルシステム

スキルはStarnion AIエージェントの機能モジュールです。各スキルは特定の機能を担当する独立したツールで、必要に応じてオン/オフを切り替えることができます。

---

## スキルとは？

スキルは、AIが単に会話するだけでなく、実際に**実行**することを可能にする機能単位です。

例えば：
- `天気はどうですか？` → **weather** スキルが天気APIを呼び出す
- `昼食1,200円` → **finance** スキルが費用管理に記録する
- `レシートの写真` → **image** スキルが金額を抽出 → **finance** スキルが自動的に記録する

ユーザーがスキル名を明示的に指定しなくても、AIがコンテキストを読み取って適切なスキルを自動的に選択します。

---

## スキルの有効化/無効化

**設定 > スキル**で各スキルを個別に切り替えることができます。

- 無効化されたスキルは、AIが関連するリクエストを受け取っても実行されません。
- 一部のコアスキル（システムスキル）は無効にできません。
- スキルの設定はアカウントごとに保存されます。

---

## スキル詳細パネル

**設定 > スキル**の各スキルカードには **ⓘ** ボタンがあります。クリックするとサイドパネルが開き、以下の情報が表示されます：

- **説明** — スキルの機能
- **トリガーキーワード** — メッセージに含まれるとスキルが自動的に起動される単語
- **使用例** — チャットにそのまま送信できるコピー可能な例

詳細パネルからスキルを直接有効化または無効化することもできます。

---

## 全スキルリスト

### 財務管理

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `finance` | 費用管理 | 収入と支出を自動的に記録します。「昼食1,200円」や「給料が入った」などのメッセージに応答します | ✓ |
| `budget` | 予算管理 | カテゴリー別に月次予算を設定し、変更して使用状況を確認します | ✓ |
| `pattern` | 支出パターン分析 | 曜日別の支出傾向、定期的な請求、支出ペースをバックグラウンドで自動的に分析します | ✓ |
| `proactive` | プロアクティブ通知 | 予算超過、異常な支出などのAI主導のアラート | ✓ |
| `currency` | 為替レート | リアルタイムの為替レート検索と通貨換算 | ✓ |

### パーソナル

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `diary` | 日記 | 日常生活、感情、思考を記録します。「今日は最高の気分」や「ミーティングが長引いた」などの非財務的なメッセージに応答します | ✓ |
| `memo` | メモ | タイトルとタグでメモを保存し、リストを確認・削除できます | ✓ |
| `goals` | 目標管理 | 財務目標、To-Do、習慣、個人目標を設定して進捗を追跡します | ✓ |
| `dday` | Dデイ | 重要な日付までの残り日数を追跡します。年次繰り返しをサポートします | ✓ |
| `reminder` | リマインダー | 特定の時間に一度限りのアラートをスケジュールします。「明日午前9時のミーティングをリマインドして」 | ✓ |
| `schedule` | スケジュール | 定期的または一度限りのアラートスケジュールを作成・管理します。繰り返しアラートをサポートします | ✓ |
| `memory` | メモリ検索 | 過去の会話、費用記録、日記エントリ、ドキュメントにわたる統合セマンティック検索 | ✓ |

### メディア

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `image` | 画像分析と生成 | 画像を分析し、AI画像を生成・編集し、レシートを自動解析します | ✓ |
| `audio` | 音声変換 | 音声テキスト変換（STT）とテキスト音声変換（TTS） | ✓ |
| `video` | 動画分析 | 動画コンテンツの要約、シーンの説明、AI画像スライドショー動画の生成 | - |
| `documents` | ドキュメント管理 | PDF/Wordドキュメントを解析してベクターDBに保存し、新しいドキュメントを生成します | ✓ |
| `qrcode` | QRコードジェネレーター | URLまたはテキストからQRコード画像を生成します | ✓ |

### 検索

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `websearch` | ウェブ検索 | Tavily APIを通じて最新の情報をインターネットで検索し、ウェブページのコンテンツを取得します | ✓ |
| `naver_search` | Naver検索 | Naver Open APIを通じてショッピング、ブログ、ニュース、本、知識iN、ローカル結果を検索します | - |
| `weather` | 天気 | 現在の天気と予報を表示します | ✓ |
| `summarize` | 要約 | URLウェブページまたはテキストのAI要約。簡潔/詳細/箇条書きスタイルをサポートします | ✓ |

### ユーティリティ

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `calculator` | 電卓 | 数式を計算します。sin/cos/logなどの数学関数をサポートします | ✓ |
| `translate` | 翻訳 | テキストを指定した言語に翻訳します。自動言語検出をサポートします | ✓ |
| `currency` | 通貨換算 | リアルタイムの為替レートに基づく通貨換算 | ✓ |
| `timezone` | 世界時刻 | 世界中の都市の現在時刻を表示し、タイムゾーン間で変換します | ✓ |
| `unitconv` | 単位変換 | 長さ、重量、温度、速度、面積などの単位を変換します | ✓ |
| `color` | カラー変換 | HEX、RGB、HSLのカラーコードを相互変換します | ✓ |
| `encode` | エンコーディング | Base64、URL、HTMLのエンコードとデコード | ✓ |
| `hash` | ハッシュ | MD5、SHA-1、SHA-256ハッシュ値を生成します | ✓ |
| `wordcount` | 文字数カウント | 文字数、単語数、文数、バイト数を分析します | ✓ |
| `random` | ランダム | ランダム選択、数字の抽選、コイン投げ、サイコロ投げ | ✓ |
| `ip` | IP検索 | IPアドレスまたはドメインの位置情報とISP情報を検索します | ✓ |

### 連携

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `google` | Google連携 | Googleカレンダー、ドキュメント、タスク、ドライブ、Gmailと連携します。OAuth2認証が必要です | - |
| `browser` | ブラウザ操作 | 自動化されたウェブブラウザ制御：URLナビゲーション、クリック、スクリーンショット、ページ読み込み | - |
| `notion` | Notion連携 | Notionページの検索・作成・閲覧、ブロックの追記、データベースのクエリ、プロパティの更新。インテグレーショントークンが必要。 | - |
| `github` | GitHub連携 | リポジトリ・issue・PRの閲覧、issueの作成、コード検索。個人アクセストークンが必要。 | - |

### その他

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `horoscope` | 星座占い | 12星座のいずれかの今日の星座占いを表示します | ✓ |

### システム

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `usage` | 使用状況サマリー | AIの使用統計（リクエスト数、トークン数、コスト、モデル別・日別の内訳）を確認します | ✓ |

### 開発

| スキルID | スキル名 | 説明 | デフォルト有効 |
|---------|---------|------|-------------|
| `coding_agent` | コーディングエージェント | Claude Code CLIにコーディングタスクを委任します：機能の実装、リファクタリング、テスト作成、README生成 | - |

---

## 自然言語によるスキルの呼び出し

スキル名を明示的に指定する必要はありません。自然に話しかけるだけで、AIが適切なスキルを選択します。

```
私: 50,000ウォンはドルでいくらですか？
Bot: 現在の為替レート（1,340 KRW/USD）で、50,000ウォン ≈ 37.31ドルです。

私: ロンドンは今何時ですか？
Bot: ロンドン（GMT）の現在時刻は午前6時42分です。

私: 今日は本当にひどい気分です。上司に怒られました。
Bot: それは辛い一日でしたね。日記に記録しました。

私: 明日の午後3時に医者の予約をリマインドしてください。
Bot: 「医者の予約」のリマインダーを明日の午後3時に設定しました。

私: この画像には何がありますか？（写真を添付）
Bot: 画像を分析しました。コーヒーショップのレシートのようです — アメリカーノが450円です。

私: https://news.ycombinator.com を要約してください。
Bot: そのページを要約しました。現在のトップ投稿：...
```

---

## スキルの権限

一部のスキルは外部APIキーまたはOAuth認証が必要です。

| スキル | 必要な設定 |
|-------|-----------|
| `weather` | 天気APIキー（環境変数） |
| `websearch` | Tavily APIキー |
| `naver_search` | Naver クライアントID / シークレット |
| `google` | Google OAuth2認証完了 |
| `image` | OpenAI APIキー（画像生成用） |
| `currency` | 為替レートAPIキー |
| `notion` | Notionインテグレーショントークン |
| `github` | GitHub個人アクセストークン |
| `coding_agent` | Claude Code CLI（サーバー環境） |

APIキーの設定はサーバー管理者が `.env` ファイルで行います。

---

## 今後のスキル（フェーズ2）

以下のスキルが開発ロードマップに含まれています：

| スキル | 説明 |
|-------|------|
| `video` | リアルタイムストリーミング分析 |
| `slack` | Slackチャンネル連携 |

---

## よくある質問

**Q. 無効化されたスキルのデータは削除されますか？**
いいえ。スキルを無効にしても既存のデータは削除されません。再度有効にすると、すべての以前のデータへのアクセスが復元されます。

**Q. システムスキルとは何ですか？**
`memory` や `proactive` などのエージェントのコア操作に必要なスキルです。これらのスキルは無効にできません。

**Q. 複数のスキルを同時に使用できますか？**
はい。1つのメッセージで複数のスキルを順番に起動できます。例えば、レシート画像を送信すると、`image` スキルが金額を認識し、`finance` スキルが費用管理に自動的に記録します。
