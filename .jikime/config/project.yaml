# Project Settings
# Basic project metadata

project:
  name: "starnion"    # Project name (set during jikime init)
  description: ""             # Project description
  type: ""                    # Project type: web_browser_app, mobile_app, cli_tool, backend_api
  locale: "ko_KR"        # Auto-derived from conversation_language (ko→ko_KR, en→en_US, etc.)
  created_at: ""              # Auto-set during init
  initialized: false          # Auto-set during init

github:
  profile_name: "jikime"  # GitHub username (e.g., @GoosLab)
