# Required
GEMINI_API_KEY=your-gemini-api-key
# TELEGRAM_BOT_TOKEN is no longer used; configure bots per-user via the Channels page.
JWT_SECRET=your-jwt-secret-min-32-chars

# Optional (defaults shown)
POSTGRES_PASSWORD=jiki_dev_password
