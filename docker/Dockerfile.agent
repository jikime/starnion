FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock* ./
RUN uv sync --no-dev

COPY src/ src/

EXPOSE 50051

CMD ["uv", "run", "python", "-m", "starpion_agent"]
